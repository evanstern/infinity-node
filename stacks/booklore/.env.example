# BookLore Environment Configuration
# Service: BookLore (Digital Library)
# VM: 103 (misc)
# Generated: 2025-12-22
# Documentation: https://github.com/booklore-app/BookLore

# ==============================================================================
# CORE SETTINGS
# ==============================================================================

# Timezone (default: America/New_York)
TZ=America/New_York

# User/Group IDs (match host user for proper file permissions)
PUID=1000
PGID=1000

# ==============================================================================
# SECRETS (Store in Vaultwarden)
# ==============================================================================

# Database password for booklore user
# Retrieve from: infinity-node/vm-103-misc/booklore-secrets
# Field: db_password
DB_PASSWORD=PLACEHOLDER_RETRIEVE_FROM_VAULTWARDEN

# MariaDB root password
# Retrieve from: infinity-node/vm-103-misc/booklore-secrets
# Field: mysql_root_password
MYSQL_ROOT_PASSWORD=PLACEHOLDER_RETRIEVE_FROM_VAULTWARDEN

# ==============================================================================
# VAULTWARDEN STORAGE
# ==============================================================================
# Store secrets in Vaultwarden using:
#
# export BW_SESSION=$(bw unlock --raw)
#
# Generate secrets:
#   DB_PASSWORD=$(openssl rand -base64 36 | tr -d '=/+' | head -c 32)
#   MYSQL_ROOT_PASSWORD=$(openssl rand -base64 36 | tr -d '=/+' | head -c 32)
#
# Create Vaultwarden entry:
# ./scripts/secrets/create-secret.sh "booklore-secrets" "vm-103-misc" "" \
#   '{"service":"booklore","vm":"103","db_password":"<generated>","mysql_root_password":"<generated>"}'
#
# Retrieve secrets using:
# ./scripts/secrets/get-vw-secret.sh "booklore-secrets" "vm-103-misc" "db_password"
# ./scripts/secrets/get-vw-secret.sh "booklore-secrets" "vm-103-misc" "mysql_root_password"

