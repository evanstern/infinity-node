# Traefik Configuration Path
# Path to directory containing traefik.yml and dynamic.yml configuration files
# These files must be manually placed on the VM in a persistent location
# Default: /opt/traefik/config (recommended)
CONFIG_PATH=/opt/traefik/config
