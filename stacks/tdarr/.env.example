# Tdarr Stack Environment Variables
# Copy to .env and fill in actual values
# DO NOT commit .env to git (contains secrets)

# Timezone (America/New_York for US Eastern Time)
TZ=America/New_York

# Config directory path (on NAS for persistence)
# Example: /mnt/nas/configs/tdarr
CONFIG_PATH=/path/to/tdarr/config

# Media library paths (NFS mounts)
# Movies library
MEDIA_PATH_MOVIES=/mnt/video/Movies

# TV Shows library
MEDIA_PATH_TV=/mnt/video/TV

# Transcode cache directory
# Recommend tmpfs or fast local SSD for temp transcoding files
# Example: /mnt/nas/tdarr/cache or /tmp/tdarr
TRANSCODE_CACHE=/path/to/transcode/cache

# MongoDB root password
# Generate with: openssl rand -base64 32
# Store in Vaultwarden: vm-100-emby collection, item "tdarr-mongodb-root"
MONGODB_ROOT_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD

