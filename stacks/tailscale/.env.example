# Tailscale stack env defaults
TS_HOSTNAME=vm-100
# Advertise routes only on the chosen subnet router (leave blank elsewhere)
TS_ROUTES=
# Whether to let Tailscale manage DNS (default false to keep local DNS)
TS_ACCEPT_DNS=false
# Advertise exit node from this host (true/false)
TS_EXIT_NODE=false
# Extra args to pass to `tailscale up` (e.g., "--shields-up")
TS_EXTRA_ARGS=
# Host path for tailscale state (persist across restarts)
TS_STATE_DIR_HOST=/home/evan/config/tailscale
# Tun device path (normally leave default)
TS_TUN_DEVICE=/dev/net/tun
# Auth key: set either TS_AUTHKEY (inline) or TS_AUTHKEY_FILE (path on host)
TS_AUTHKEY=
TS_AUTHKEY_FILE=
