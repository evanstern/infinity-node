# Watchtower Environment Configuration
# Service: Watchtower (Automatic Docker Container Updater)
# VM: 103 (misc) - Also deployed on VM 100, 101, 102
# Generated: 2025-10-26

# ==============================================================================
# UPDATE SCHEDULE
# ==============================================================================

# Cron format for update checks
# Default: Daily at midnight UTC
SCHEDULE=0 0 * * *

# ==============================================================================
# CONTAINER UPDATE OPTIONS
# ==============================================================================

# Remove old images after update (recommended: true)
CLEANUP=true

# Remove volumes when cleaning (recommended: false)
REMOVE_VOLUMES=false

# Restart containers one by one instead of all at once
ROLLING_RESTART=true

# Update stopped containers (recommended: false)
INCLUDE_STOPPED=false

# Only monitor for updates, don't actually update (testing mode)
MONITOR_ONLY=false

# Enable lifecycle hooks for container start/stop
ENABLE_LIFECYCLE_HOOKS=true

# ==============================================================================
# OPTIONAL: LABEL FILTERING
# ==============================================================================

# Only update containers with specific label (com.centurylinklabs.watchtower.enable=true)
# Uncomment to enable label-based filtering
# LABEL_ENABLE=true

# ==============================================================================
# OPTIONAL: EMAIL NOTIFICATIONS
# ==============================================================================

# Uncomment and configure to receive email notifications about updates
# NOTIFICATION_EMAIL_FROM=watchtower@your-domain.com
# NOTIFICATION_EMAIL_TO=admin@your-domain.com
# NOTIFICATION_EMAIL_SERVER=smtp.gmail.com
# NOTIFICATION_EMAIL_PORT=587
# NOTIFICATION_EMAIL_USER=your-email@gmail.com
# NOTIFICATION_EMAIL_PASSWORD=your-app-password
# NOTIFICATION_EMAIL_TLS=true

# ==============================================================================
# OPTIONAL: DOCKER REGISTRY AUTHENTICATION
# ==============================================================================

# Uncomment if pulling from private registries
# REGISTRY_USER=your-registry-username
# REGISTRY_PASSWORD=your-registry-password

# ==============================================================================
# OPTIONAL: HTTP API
# ==============================================================================

# Enable HTTP API for external control
# ENABLE_HTTP_API=false
# HTTP_API_TOKEN=your-secure-token

# ==============================================================================
# NOTES
# ==============================================================================
# - Watchtower watches all running containers by default
# - Use LABEL_ENABLE=true to selectively update containers
# - Schedule uses cron format: minute hour day month weekday
# - Consider MONITOR_ONLY=true for testing before enabling auto-updates
# - CLEANUP=true is recommended to avoid disk space issues
