# LazyLibrarian Stack Configuration

# Timezone
TZ=America/New_York

# User/Group IDs
PUID=1000
PGID=1000

# Network
LAZYLIBRARIAN_PORT=5299

# Storage Paths
# Configuration directory (local VM storage for DB performance)
CONFIG_PATH=/home/evan/.config/lazylibrarian

# Downloads directory (typically shared with download clients via NFS)
DOWNLOADS_PATH=/media/downloads

# Books directory (NFS mount from VM 103 where Calibre library lives)
BOOKS_PATH=/mnt/calibre-library
