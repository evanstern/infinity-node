# Emby Media Server Environment Configuration
# Copy this file to .env and update the values for your environment

# Timezone
TZ=America/Toronto

# Path Configuration
CONFIG_PATH=/home/evan/projects/infinity-node/stacks/emby/config
MEDIA_PATH=/mnt/video/Video

# Network Configuration
# Emby uses host network mode for better performance and device discovery
# Default web UI port: 8096 (HTTP), 8920 (HTTPS)

# Secrets
# No secrets required - Emby manages authentication internally via web UI
# Admin account is created on first run

# Hardware Transcoding (Optional)
# To enable GPU transcoding, uncomment the deploy section in docker-compose.yml
# Requires NVIDIA GPU and nvidia-docker runtime installed on the host

# Notes:
# - MEDIA_PATH should point to your media library (movies, TV shows, etc.)
# - CONFIG_PATH stores Emby database, metadata, and configuration
# - Network mode 'host' allows Emby to discover DLNA devices and clients
# - See README.md for hardware transcoding setup instructions
