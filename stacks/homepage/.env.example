# Homepage Environment Configuration
# Service: Homepage (Dashboard)
# VM: 103 (misc)
# Generated: 2025-10-26

# ==============================================================================
# NON-SECRET CONFIGURATION
# ==============================================================================

# Web Interface Port (external)
PORT=3001

# Allowed hosts for access (comma-separated)
# Update with DNS name (or IP if DNS not available)
HOMEPAGE_ALLOWED_HOSTS=homepage.local.infinity-node.com:3001

# User/Group IDs (for file permissions)
PUID=1000
PGID=1000

# Configuration directory
# Default: Relative path (resolves to /data/compose/<stack-id>/stacks/homepage/config/homepage when deployed via Portainer)
# Can be changed to absolute path if needed: /data/compose/<stack-id>/stacks/homepage/config/homepage
# Update via: ./scripts/infrastructure/update-stack-env.sh "portainer-api-token-vm-103" "shared" <stack-id> 3 --env "CONFIG_PATH=/path/to/config"
CONFIG_PATH=./config/homepage

# ==============================================================================
# NOTES
# ==============================================================================
# - No secrets required for basic operation
# - Dashboard configuration is done via YAML files in CONFIG_PATH
# - Mounts Docker socket to monitor containers
# - Widgets can integrate with various services (Radarr, Sonarr, etc.)
# - Update HOMEPAGE_ALLOWED_HOSTS if VM IP changes
# - CONFIG_PATH can be updated via update-stack-env.sh script after initial deployment
# - Relative path (./config/homepage) works correctly with Portainer Git deployment
# - Config files are stored in git at: stacks/homepage/config/homepage/
